<!DOCTYPE html>
<html lang="en">
<head>
    <title>Attendance Report</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Attendance Report for <%= currentMonth %></h1>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Employee</th>
                    <th>Present</th>
                    <th>Absent</th>
                    <th>On Leave</th>
                </tr>
            </thead>
            <tbody>
                <% Object.values(summary).forEach(employee => { %>
                    <tr>
                        <td><%= employee.name %></td>
                        <td><%= employee.present %></td>
                        <td><%= employee.absent %></td>
                        <td><%= employee.onLeave %></td>
                    </tr>
                <% }); %>
            </tbody>
            <a href="/reports/attendance/export-csv" class="btn btn-primary mb-3">Export Attendance as CSV</a>
        </table>
    </div>
</body>
</html>
